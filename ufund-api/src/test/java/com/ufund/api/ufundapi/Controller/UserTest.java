package com.ufund.api.ufundapi.Controller;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.util.ArrayList;
import java.util.List;

import org.junit.jupiter.api.Test;

import com.ufund.api.ufundapi.model.Need;
import com.ufund.api.ufundapi.model.User;

public class UserTest {
    
    // @Test
    // public void testUserToString(){

    //     int id = 1;
    //     String userName = "carla";
    //     List<Need> list = List.of();

    //     String expectedString = String.format(User.STRING_FORMAT_USER,id,userName,list);

    //     User user = new User(1, "carla");

    //     String actual = user.toString();

    //     assertEquals(expectedString,actual);


    // }

    // @Test
    // public void voidUserWithFundingBasket(){
    // // List<Need> list = new ArrayList<>();
    // // list.add();

    // // Set up the expected string
    // String expectedString = "User [id=1, userName=carla, fundingBasket=Need [name=Item 1, description=Description 1], Need [name=Item 2, description=Description 2]]";

    // User user = new User(1, "carla");
    // user.setFundingBasket(new Need(0, "Item 1", 10, "Description 1", 10, "hello")); // Add the list of Needs to the user

    // String actual = user.toString();

    // assertEquals(expectedString, actual);


    // }

    
}
